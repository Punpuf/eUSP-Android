<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/dialogQrcodeConstraintLayout"
    android:layout_width="@dimen/code_dialog_width"
    android:layout_height="@dimen/code_dialog_height"
    android:layout_gravity="center"
    android:layout_margin="@dimen/spacing_normal"
    android:background="@drawable/bg_dialog_code">

    <ImageView
        android:id="@+id/dialogQrcodeQrcodeIv"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:contentDescription="@string/card_qrcode_content_description"
        android:background="@color/white"
        android:scaleType="fitCenter"
        android:layout_margin="@dimen/padding_large"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="1:1" />

</androidx.constraintlayout.widget.ConstraintLayout>